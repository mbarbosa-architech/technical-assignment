import { FilterEntriesPipe } from './filter-entries.pipe';

describe('FilterEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
